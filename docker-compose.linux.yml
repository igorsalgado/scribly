# Linux override — passes the host audio device to the worker container.
# Used automatically by the Makefile on Linux.
services:
  scribly-worker:
    devices:
      - /dev/snd:/dev/snd
    group_add:
      - audio
